<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MySplashScreen" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/img_splash</item>
        <item name="postSplashScreenTheme">@android:style/Theme.Black.NoTitleBar</item> <!-- This will switch back to your given theme once the splash screen is gone-->
    </style>
</resources>
